# If you want to point to a different API base
# Web local dev can stay empty because Vite proxy already targets localhost:5000.
# For Android/iOS or a physical device, set this to your backend URL, for example:
# VITE_API_BASE=http://192.168.0.25:5000
VITE_API_BASE=
